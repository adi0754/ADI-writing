# H5 AI Writer Environment Variables

# Development server port
VITE_PORT=5173

# API Base URLs (if needed for future backend integration)
# VITE_API_URL=http://localhost:3000

# Note: API keys are configured through the application UI
# and stored in the browser's localStorage for security
